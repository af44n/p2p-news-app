# Local Storage Shim

A super simple fake `localStorage` for when you're running in the CLI or Bare.

## What it does

- Gives you the classic `getItem`, `setItem`, `removeItem`, and `clear`.
- Just saves it to a `.json` file so your stuff is still there when you restart.
- Means we can use the exact same state code in the browser and CLI without rewriting everything.

## Usage

```javascript
// Automatically applied when running in a non-browser environment
require('local-storage-shim')

// Now you can use localStorage safely anywhere
localStorage.setItem('username', 'alice')
const name = localStorage.getItem('username')
```
